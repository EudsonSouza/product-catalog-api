{
  "ConnectionStrings": {
    "DefaultConnection": "Host=${DB_HOST};Database=${DB_NAME};Username=${DB_USERNAME};Password=${DB_PASSWORD};Port=${DB_PORT}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "${LOG_LEVEL_DEFAULT}",
      "Microsoft.AspNetCore": "${LOG_LEVEL_ASPNETCORE}"
    }
  }
}
